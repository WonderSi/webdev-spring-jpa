info:
  name: GET_id
  type: http
  seq: 4

http:
  method: GET
  url: "{{base_url}}{{path}}/users/1"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
