info:
  name: POST
  type: http
  seq: 6

http:
  method: POST
  url: "{{base_url}}{{path}}/users"
  body:
    type: json
    data: |-
      {
        "email": "test@example.com",
        "firstName": "Ivan",
        "lastName": "Ivanov",
        "isActive": true
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
