info:
  name: PUT
  type: http
  seq: 6

http:
  method: PUT
  url: "{{base_url}}{{path}}/users/1"
  body:
    type: json
    data: |-
      {
        "email": "updated@example.com",
        "firstName": "Mikhail",
        "lastName": "Mikhailov",
        "isActive": false
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
