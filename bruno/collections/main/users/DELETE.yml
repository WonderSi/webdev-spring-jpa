info:
  name: DELETE
  type: http
  seq: 6

http:
  method: DELETE
  url: "{{base_url}}{{path}}/users/3"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
