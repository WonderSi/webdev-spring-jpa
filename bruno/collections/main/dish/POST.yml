info:
  name: POST
  type: http
  seq: 9

http:
  method: POST
  url: "{{base_url}}{{path}}/dishes"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
